# .github/workflows/greengage-reusable-tests-regression.yml
name: Greengage Reusable Regression Tests on GitHub

# Environment variables
env:
  COMPOSE_HTTP_TIMEOUT: 400
  DOCKER_COMPOSE: "docker compose"

# Trigger for reusable workflow
on:
  workflow_call:
    inputs:
      version:
        description: 'Greengage version (e.g., 6 or 7)'
        required: true
        type: string
      target_os:
        description: 'Target OS (e.g., ubuntu)'
        required: true
        type: string
      target_os_version:
        description: 'Target OS version (e.g., 24.04)'
        required: false
        type: string
        default: ''
    secrets:
      ghcr_token:
        description: 'GitHub token for GHCR access'
        required: true

jobs:
  optimizer:  # Regression tests
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        optimizer: [orca, postgres]
    permissions:
      contents: read  # Explicit for default behavior
      packages: read  # Explicit for GHCR access clarity
      actions: write  # Required for cache and artifact upload
    steps:
      - name: Maximize disk space
        uses: greengagedb/greengage-ci/.github/actions/maximize-disk-space@v19

      - name: Enable Docker IPv6
        uses: greengagedb/greengage-ci/.github/actions/enable-docker-ipv6@v51

      - name: Restore & Load SHA image
        uses: greengagedb/greengage-ci/.github/actions/restore-load-image@v25
        with:
          version: ${{ inputs.version }}
          target_os: ${{ inputs.target_os }}
          target_os_version: ${{ inputs.target_os_version }}

      - name: Checkout Greengage repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.ref }}
          submodules: recursive

      - name: Regression tests with optimizer '${{ matrix.optimizer }}'
        uses: greengagedb/greengage-ci/.github/actions/tests/regression@v49
        with:
          image: ghcr.io/${{ github.repository }}/ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}:${{ github.sha }}
          optimizer: ${{ matrix.optimizer }}
          target_os: ${{ inputs.target_os }}
          target_os_version: ${{ inputs.target_os_version }}
          log_name: regression_ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}_${{ matrix.optimizer }}

      - name: Collect logs optimizer ${{ matrix.optimizer }}
        if: always()
        uses: greengagedb/greengage-ci/.github/actions/collect-logs@v26
        with:
          log_path_prefix: regression_ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}_${{ matrix.optimizer }}

      - name: Upload regression optimizer ${{ matrix.optimizer }} artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression_ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}_${{ matrix.optimizer }}
          path: |
            /mnt/logs
            coverage/coverage-html/
            coverage/coverage-data
          retention-days: 7
          if-no-files-found: warn  # Warning if no artifacts are found
